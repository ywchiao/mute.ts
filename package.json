{
  "name": "mute",
  "version": "0.1.0",
  "description": "An electron based  mud client for Muted.",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "format": "prettier-eslint --write 'src/**/*.ts'",
    "lint": "eslint --fix src > log/eslint.log",
    "start": "npm run build && electron ./out/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ywchiao/mute.ts.git"
  },
  "keywords": [
    "Electron",
    "mud",
    "typescript"
  ],
  "author": "Yiwei Chiao",
  "license": "MIT",
  "devDependencies": {
    "@types/css-font-loading-module": "0.0.4",
    "@typescript-eslint/eslint-plugin": "^4.2.0",
    "@typescript-eslint/parser": "^4.2.0",
    "electron": "^10.1.2",
    "eslint": "^7.9.0",
    "papercss": "^1.8.0",
    "prettier-eslint-cli": "^5.0.0",
    "typescript": "^4.0.3",
    "wired-elements": "^2.1.2"
  },
  "dependencies": {}
}
